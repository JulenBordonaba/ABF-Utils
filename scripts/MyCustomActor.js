class MyCustomActor extends game.system.entityClasses.Actor {
    constructor(...args) {
        super(...args);
        // add your own custom constructor logic here
    }

    myCustomMethod() {
        // add your own custom method logic here
    }

    static myStaticMethod() {
        // add your own custom static method logic here
    }
}
